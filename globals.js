console.log("GLOBALS CARGADO");
/* =========================
   VARIABLES GLOBALES
========================= */

let zoom = 1;

let offsetCanvasX = 0;

let offsetCanvasY = 0;

let seleccionCable = null;

let dragging = null;

let offsetX = 0;

let offsetY = 0;

let energiaOffset = 0;

let cortoCircuito = false;

let colorCable = "red";

let lastTouchDistance = 0;

let lastTouchCenterX = 0;

let lastTouchCenterY = 0;